#include <bits/stdc++.h>
using namespace std;

#define mod 1e9 + 7

int main()
{
    int t = 0, i = 0;
    cin >> t;
    for (i = 0; i < t; i++)
    {
        int n = 0, m = 0;
        cin >> n >> m;
        long long int total_tuples = pow(2, (long long int)m * n);
    }
    return 0; //
}